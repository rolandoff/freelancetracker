# Production Environment Variables
# Copy this to .env.production and fill with your actual values

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# IMPORTANT NOTES:
# 1. These variables are compiled into the JavaScript bundle
# 2. Never commit .env.production to git (it's in .gitignore)
# 3. Only use the ANON key, never the SERVICE_ROLE key in frontend
# 4. Configure RLS (Row Level Security) in Supabase for security
# 5. Add your domain to Supabase allowed URLs in dashboard

# After setting these values:
# 1. Run: npm run build
# 2. Upload contents of dist/ folder to your LWS server
# 3. Test the application
